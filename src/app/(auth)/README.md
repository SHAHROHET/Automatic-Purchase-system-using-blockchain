# (auth) Route Group

This folder contains pages related to **authentication** — login, register, and password reset.

Pages inside this folder share a simple centered layout (no sidebar).

Current pages:
- `../login/` — Login page (currently at `/login`)

> As the project grows, move `../login/` into here and add a shared `layout.tsx` for the auth layout.
